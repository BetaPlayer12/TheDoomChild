using UnityEngine;
#if UNITY_EDITOR
#endif

namespace DChild.Inputs
{
    public enum InputKey
    {
        Left,
        Right,
        Up,
        Down,
        [HideInInspector]
        _COUNT
    }
}
